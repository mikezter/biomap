<% title "Edit Foto" %>
<% content_for :head do %>
	<%= GMap.header %>
	<!-- -->
	<%= @map.to_html %>
	<!-- -->

<% end %>

  <%= render :partial => 'form' %>

<%= @map.div(:width => 600,:height => 400) %>

<p>
  <%= link_to "Show", @foto %> |
  <%= link_to "View All", fotos_path %>
</p>
